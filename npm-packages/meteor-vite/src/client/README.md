# Meteor-Vite [Client]

This directory contains modules that will be included alongside the Meteor client bundle. Primarily to provide 
runtime validation of Meteor package stubs.

In other words, everything here can be accessed safely within the browser.