---
"meteor-vite": minor
---

Added deprecation note to the `assetsBaseUrl` field in Meteor-Vite's plugin options. The field served the same purpose as the [`base`](https://vite.dev/config/shared-options.html#base) field from Vite's default config and is redundant.
